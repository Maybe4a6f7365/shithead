import { useState } from 'react'
import { useSPGame } from '../sp/SPSinglePlayer'
import { createRoom } from '../net/useMultiplayerRoom'
interface Props{onJoin:(roomId:string)=>void}
export function MultiplayerLobby({onJoin}:Props){const[name,setName]=useState(''),[joinCode,setJoinCode]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null)
 const create=async()=>{const n=name.trim();if(!n)return setError('Enter your name');setBusy(true);setError(null);try{const id=await createRoom();sessionStorage.setItem(`shithead:name:${id}`,n);sessionStorage.setItem(`shithead:intent:${id}`,'create');onJoin(id)}catch(e){setError(e instanceof Error?e.message:'Failed to create room')}finally{setBusy(false)}}
 const join=()=>{const n=name.trim(),id=joinCode.toUpperCase();if(!n)return setError('Enter your name');if(!/^[A-Z0-9]{6}$/.test(id))return setError('Invalid code (6 chars)');sessionStorage.setItem(`shithead:name:${id}`,n);sessionStorage.setItem(`shithead:intent:${id}`,'join');onJoin(id)}
 return <div className="min-h-screen bg-[#2d4a2b] flex items-center justify-center p-4"><div className="max-w-md w-full bg-[#faf8f3] rounded-2xl p-6 shadow-2xl"><button onClick={()=>useSPGame.getState().reset()} className="text-xs text-[#2d4a2b]/50 mb-3 hover:underline">← Back to single player</button><h1 className="text-4xl font-black text-center text-[#a23a1e] mb-2">PLAY ONLINE</h1><p className="text-center text-[#2d4a2b]/70 mb-5 text-sm">Real-time multiplayer via WebSocket</p><div className="space-y-4"><div><label className="text-xs font-bold uppercase tracking-wider text-[#2d4a2b]/70 mb-1 block">Your Name</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="Player 1" maxLength={32} className="w-full px-3 py-2 rounded-lg border border-[#2d4a2b]/20 bg-white"/></div><button onClick={create} disabled={busy||!name.trim()} className="w-full py-3 rounded-xl bg-[#a23a1e] text-[#faf8f3] font-black shadow-lg active:scale-95 transition disabled:opacity-50">{busy?'CREATING…':'CREATE NEW ROOM'}</button><div className="flex items-center gap-2 text-xs text-[#2d4a2b]/40"><div className="flex-1 h-px bg-[#2d4a2b]/20"/><span>OR JOIN</span><div className="flex-1 h-px bg-[#2d4a2b]/20"/></div><input value={joinCode} onChange={e=>setJoinCode(e.target.value.toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,6))} placeholder="ROOM CODE" maxLength={6} className="w-full px-3 py-2 rounded-lg border border-[#2d4a2b]/20 bg-white text-center font-mono text-xl tracking-widest"/><button onClick={join} disabled={!name.trim()||joinCode.length!==6} className="w-full py-3 rounded-xl bg-[#2d4a2b] text-[#faf8f3] font-black shadow-lg active:scale-95 transition disabled:opacity-50">JOIN</button></div>{error&&<div className="mt-3 p-2 bg-[#a23a1e]/10 text-[#a23a1e] text-sm rounded text-center">{error}</div>}</div></div>}
