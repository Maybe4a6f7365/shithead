{
  "name": "shithead-cloudflare-deploy",
  "private": true,
  "version": "1.0.0",
  "description": "Repository-root deployment wrapper for Cloudflare Workers Builds.",
  "scripts": {
    "postinstall": "npm --prefix app ci && npm --prefix app run deploy:build"
  },
  "dependencies": {
    "wrangler": "4.120.0"
  },
  "engines": {
    "node": ">=22"
  }
}
